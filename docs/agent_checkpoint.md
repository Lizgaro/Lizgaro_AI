# Agent Checkpoint

## Текущий источник правды

`docs/MASTER_PRD_Artem_Lizgaro_final_site_social.md`

Master PRD побеждает старый код, старые PRD, старые README и прошлые версии текстов.

## Статус структуры

- Рабочая папка сайта: `C:\LA\life-os-v0.3\Lizgaro_site`.
- Старое имя `portfolio-site` больше не используется.
- Старые PRD и черновики перенесены в `archive/old_prd/`.
- Активная документация сайта лежит в `docs/`.

## Что уже найдено

- Проект: Next.js App Router, React 19, Tailwind CSS, TypeScript.
- Главная собирается в `app/page.tsx`.
- Основные тексты лежат в `data/` и частично захардкожены в компонентах.
- Команды проверки: `npm run lint`, `npm run build`.

## Запреты из master PRD

- Не продавать Life OS как услугу.
- Не использовать подкастеров как ЦА, направление или оффер.
- Не возвращать CTA `Хочу собрать свой AI-мозг`.
- Не позиционировать сайт как набор отдельных услуг `сайт / бот / AI`.
- Не использовать старые PRD даже как дополнительный контекст.

## Следующий этап

## Сделано 2026-05-20

- Создана отдельная рабочая папка сайта: `C:\LA\life-os-v0.3\Lizgaro_site`.
- Next.js-проект перенесён из старой папки `portfolio-site` вместе с `.git`, кодом, зависимостями и текущими правками.
- Master PRD скопирован в `docs/MASTER_PRD_Artem_Lizgaro_final_site_social.md`.
- Старые PRD и брифы перенесены в `archive/old_prd/`.
- Создан `docs/README.md` с правилами: master PRD — единственный источник правды, архив не использовать.
- Главная страница пересобрана по карте master PRD:
  - Hero.
  - Диагноз / PROJECT SCAN.
  - Три задачи.
  - Сайт / лендинг и AI / автоматизация.
  - Процесс.
  - Кейсы.
  - Proof of work.
  - Блог.
  - Соцсети.
  - FAQ.
  - Финальный CTA.
- Удалены активные упоминания `AI-мозг`, CTA `Хочу собрать свой AI-мозг`, `контент-системы` как оффера и старые ссылки на `portfolio-site`.
- Life OS оставлен только как внутренняя система и proof of work.
- Исправлен баг формы: после отправки форма больше не падает на `event.currentTarget.reset()`.

## Проверено

- `npm run lint` — проходит.
- `npm run build` — проходит.
- Playwright desktop/mobile — страница рендерится, меню работает, консоль без ошибок.
- Форма заявки отправляется и показывает success-сообщение.
- Поиск запрещённых активных формулировок вне `docs` и `archive` — чисто.

## Текущий локальный запуск

Dev-сервер запущен:

`http://127.0.0.1:3000`

## Следующий этап

1. Пройти полный визуальный polish всех секций ниже hero.
2. Добавить реальные ссылки Threads / X / Instagram, когда они будут готовы.
3. Проверить и при необходимости обновить OG-обложку главной.
4. Вынести кейсы в отдельную страницу, если сайт станет слишком длинным.
5. Настроить аналитику кликов по Telegram CTA и отправке формы.
